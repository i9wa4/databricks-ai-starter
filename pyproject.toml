[project]
name = "databricks-claude-code-starter"
version = "0.1.0"
description = "Dev Container starter template for Claude Code with Databricks"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "jupyter-databricks-kernel",
    "jupyterlab",
]
